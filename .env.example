BACKEND_URL=http://localhost:8000

# Optional local cookie verification; must match FastAPI SESSION_SECRET.
# Server only: never prefix with NEXT_PUBLIC_. Leave unset to use backend verification.
SESSION_SECRET=
